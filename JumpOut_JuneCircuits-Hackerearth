#include <stdio.h>

int main(void) {
	int N;
	int *arr = malloc(sizeof(N+1));
	scanf("%d",&N);
	int jumps=0;
	for(int i=1;i<=N;i++)
	{
		scanf("%d",&arr[i]);
	
	}
	for(int j=1;j<=N;j++)
	{
	    if((arr[j]+j)>N)
	    {
	        jumps=j;
	        break;
	    }
	}
	printf("%d",jumps);
	return 0;
}
